# Rakuten RMS Daily Data Extraction Report

**Extraction Date**: 2026-06-25  
**Target Period**: 2026-06-24 (past 1 day)  
**Status**: FAILED — Credentials not configured

---

## Extraction Attempt Summary

| Step | Description | Result |
|------|-------------|--------|
| 1 | Retrieve sales by product (past 1 day) | ❌ Not executed — no API credentials |
| 2 | Retrieve access count by product (past 1 day) | ❌ Not executed — no API credentials |
| 3 | Compile structured summary (CSV/table) | ❌ No data available |
| 4 | Store/export results | ❌ No data to export |

---

## Reason for Failure

The Rakuten RMS API requires the following credentials, which are **not present** in this repository or its environment:

| Credential | Description | Where to obtain |
|------------|-------------|-----------------|
| `serviceSecret` | サービスシークレット | RMS > システム連携 > 楽天ウェブサービス |
| `licenseKey` | ライセンスキー | RMS > システム連携 > 楽天ウェブサービス |
| `shopUrl` | ショップURL (R000000001 format) | RMS > ショップ設定 |

No `.env` file exists (only `.env.example`), and no Rakuten-related environment variables were found.

---

## API Endpoints That Would Be Called

### 1. Sales by Product (OrderSlip API or Data API)

```
GET https://api.rms.rakuten.co.jp/es/1.0/report/order/dailySalesByItem
Authorization: ESA Base64(serviceSecret:licenseKey)
Parameters:
  dateType=orderDate
  startDate=2026-06-24
  endDate=2026-06-24
```

Expected response fields per item:
- `itemName` — 商品名
- `itemUrl` — 商品管理番号
- `salesCount` — 販売数
- `salesAmount` — 売上金額
- `orderCount` — 注文件数

### 2. Access Count by Product (Access Analysis API)

```
GET https://api.rms.rakuten.co.jp/es/2.0/report/access/item
Authorization: ESA Base64(serviceSecret:licenseKey)
Parameters:
  dateType=day
  startDate=2026-06-24
  endDate=2026-06-24
```

Expected response fields per item:
- `itemName` — 商品名
- `itemUrl` — 商品管理番号
- `pageView` — ページビュー数
- `visitCount` — 訪問者数

---

## Output Format (when data is available)

### sales_YYYY-MM-DD.csv
```csv
date,item_url,item_name,order_count,sales_count,sales_amount
2026-06-24,item001,商品名A,5,10,15000
```

### access_YYYY-MM-DD.csv
```csv
date,item_url,item_name,page_view,visit_count
2026-06-24,item001,商品名A,120,95
```

### combined_YYYY-MM-DD.csv
```csv
date,item_url,item_name,order_count,sales_count,sales_amount,page_view,visit_count,conversion_rate
2026-06-24,item001,商品名A,5,10,15000,120,95,5.26
```

---

## Next Steps to Enable Extraction

1. Obtain `serviceSecret` and `licenseKey` from Rakuten RMS portal
2. Add credentials to `.env`:
   ```
   RAKUTEN_SERVICE_SECRET=your_service_secret
   RAKUTEN_LICENSE_KEY=your_license_key
   RAKUTEN_SHOP_URL=your_shop_url
   ```
3. Implement the extraction script at `scripts/rakuten/extract_daily.ts`
4. Re-run this extraction routine

---

*Generated by automated extraction routine on 2026-06-25*
